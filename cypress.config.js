const {defineConfig} = require("cypress");

module.exports = defineConfig({
    e2e: {
        // baseUrl: 'https://swapi.py4e.com/api/',
        // viewportWidth: 600,
        // viewportHeight: 800
    },
});
